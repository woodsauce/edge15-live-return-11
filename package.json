{
  "name": "edge15-genesis-balanced-28-replay-gated",
  "version": "28.0.0",
  "description": "BTC 15-minute assistant with replay-gated Genesis Balanced defaults, visual-only money score, scout-only early locks, and V27 sniper protections retained.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "start": "npx serve .",
    "vercel-build": "echo 'Static app + Vercel functions: no build step required'"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "coinbase",
    "bitcoin",
    "prediction-market",
    "vercel"
  ],
  "license": "UNLICENSED"
}
