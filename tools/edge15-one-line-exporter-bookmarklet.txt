javascript:(async()=>{const now=new Date().toISOString();const rel=k=>/edge15|btc|kalshi|commit|trade|tracker|genesis|strategy|profile|version|quality|timing|accuracy|journal/i.test(k);const parse=v=>{try{return JSON.parse(v)}catch{return v}};const ls={};for(const k of Object.keys(localStorage)){if(rel(k))ls[k]=parse(localStorage.getItem(k))}const ss={};for(const k of Object.keys(sessionStorage)){if(rel(k))ss[k]=parse(sessionStorage.getItem(k))}const payload={app:'Edge15',exportType:'browser-storage-recovery-bookmarklet',exportedAt:now,page:location.href,localStorage:ls,sessionStorage:ss};const blob=new Blob([JSON.stringify(payload,null,2)],{type:'application/json'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=`edge15-browser-storage-export-${now.replace(/[:.]/g,'-')}.json`;document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url);})();
